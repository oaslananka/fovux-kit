# Recipe 04 — Compare Architectures

1. Train or collect multiple checkpoints that target the same dataset.
2. Run `eval_compare` for checkpoint-centric comparison.
3. Run `run_compare` for training-run-centric reporting and chart output.
4. Use Fovux Studio’s comparison view to inspect the final side-by-side summary.
